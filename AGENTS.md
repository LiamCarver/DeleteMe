# Agent Instructions

- Always run `mcp__local-io-mcp__initialize_work` before any other task.
- Use only `local-io-mcp` operations for file and git actions; do not use other MCP servers or direct shell commands for edits.
